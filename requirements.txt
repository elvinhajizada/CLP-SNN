# CLP-SNN requirements
#
# PyTorch — install BEFORE this file (choose your platform):
#   GPU (CUDA 12.4):  pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124
#   CPU-only:         pip install torch==2.5.1
#
# Then install the remaining packages:
#   pip install -r requirements.txt

numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66
pandas>=2.0

# Notebooks
jupyter>=1.0
ipykernel>=6.0
